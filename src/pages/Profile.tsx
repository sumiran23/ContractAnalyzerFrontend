const Profile = () => {
  // TODO: Fetch and display user profile, allow update
  return (
    <div
      style={{
        maxWidth: 400,
        margin: "2rem auto",
        background: "#fff",
        padding: "2rem",
        borderRadius: 8,
      }}
    >
      <h2>Your Profile</h2>
      {/* Display and edit profile info here */}
    </div>
  );
};

export default Profile;
